# 运行环境：development / staging / production
APP_ENV=development

# 日志级别：DEBUG / INFO / WARNING / ERROR
APP_LOG_LEVEL=DEBUG
# 日志目录：按小时轮转，app.log 全部级别，error.log 仅 ERROR
APP_LOG_DIR=./logs

# OceanBase/MySQL 连接串（可选）
# 格式：mysql+aiomysql://user:pass@host:port/db  或 sqlite+aiosqlite:///./app.db
APP_DATABASE_URL=mysql+aiomysql://user:pass@localhost:2881/your_db

# Redis 地址（队列/缓存，可选）
APP_REDIS_URL=redis://localhost:6379/0

# 上游 LLM 服务密钥（调用外部模型时必填）
APP_LLM_API_KEY=<your-api-key>

# LLM Provider：当前默认 aliyun（阿里云通义千问）；代码仅实现 aliyun
APP_LLM_PROVIDER=aliyun
# 阿里云通义千问：模型名（如 qwen-plus、qwen-turbo）、地域、超时、重试次数
APP_LLM_MODEL=qwen-plus
APP_LLM_REGION=cn
APP_LLM_TIMEOUT=600
APP_LLM_RETRY_COUNT=3

# 签名/会话密钥（生产必填，仅通过环境变量注入）
APP_SECRET_KEY=<your-secret-key>

# API 鉴权：合法 API Key 列表，逗号分隔（开发可留空或填占位）
APP_API_KEYS=

# 百度千帆搜索（AppBuilder）：API Key、请求超时秒数
APP_BAIDU_API_KEY=<your-baidu-api-key>
APP_BAIDU_SEARCH_TIMEOUT=30

# 目录读取工具：若设置则仅允许列出该根目录下的路径（防目录穿越），空表示不限制
# APP_TOOLS_DIRECTORY_READ_ROOT=./data/files

# AI 应用输出数据目录（每次运行在其下创建子目录，如小红书笔记临时图片等）
APP_DATA_OUTPUT_DIR=./data/output

# 小红书笔记：多模态调用前图片统一压缩（长边最大像素，0 表示不缩放；质量 1–100）
APP_XHS_IMAGE_MAX_SIZE=1024
APP_XHS_IMAGE_QUALITY=85



# CrewAI：服务端无 TTY 时建议设 true，关闭 "Would you like to view your execution traces?" 等交互提示（代码中 deep_research 流程已自动设置）
# CREWAI_TESTING=true

# 应用服务端口
APP_PORT=8072


# 公众号改写：允许通过 content_path 读取本地文件的根目录白名单，
# 空表示不限制（仅开发建议），生产环境请显式指定素材目录，防目录穿越
# APP_MP_ARTICLE_READ_ROOT=/data/mp/articles
# 公众号改写：允许读取的本地原文文件后缀（小写、以点开头，逗号分隔）
APP_MP_ARTICLE_ALLOWED_SUFFIXES=.txt,.md,.markdown,.text
# 公众号改写：单个本地原文文件最大字节数，默认 2 MB
APP_MP_ARTICLE_MAX_FILE_BYTES=2097152
HUNYUAN_ENDPOINT=
HUNYUAN_API_KEY=
OPENAI_API_BASE=
OPENAI_API_KEY=
SANDBOX_ENDPOINT=